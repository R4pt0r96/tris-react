import { Typography } from '@mui/material';

const Title = () => {
  return (
    <Typography
      variant='h2'
      component='h2'
      style={{
        fontWeight: 'bold',
        color: '#00B90D',
        marginBottom: '1rem',
      }}
    >
      TRIS
    </Typography>
  );
};
export default Title;
